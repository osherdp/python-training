[tox]
skipsdist = True

[testenv:flake8]
deps =
    flake8
    -r{toxinidir}/requirements.txt

commands = flake8 .

[testenv:pytest]
deps =
    pytest
    -r{toxinidir}/requirements.txt

commands = pytest
